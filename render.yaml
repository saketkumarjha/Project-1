services:
  # Backend API Service
  - type: web
    name: hospital-backend
    env: node
    plan: free
    region: oregon
    buildCommand:  npm ci --include=dev && npm run build
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      # Server Configuration
      - key: NODE_ENV
        value: production
      
      - key: PORT
        value: 5000
      
      # Database Configuration
      - key: MONGODB_URI
        sync: false
      
      # Redis Configuration (Auto-injected from Redis service)
      - key: REDIS_URL
        fromService:
          type: redis
          name: hospital-redis
          property: connectionString
      
      # JWT Configuration
      - key: JWT_SECRET
        sync: false
      
      - key: JWT_EXPIRES_IN
        value: 24h
      
      # Session Configuration
      - key: SESSION_SECRET
        sync: false
      
      # CORS Configuration
      - key: FRONTEND_URL
        value: http://localhost:3000

  # Redis Cache & Session Store
  - type: redis
    name: hospital-redis
    plan: free
    region: oregon
    maxmemoryPolicy: allkeys-lru
    ipAllowList: []